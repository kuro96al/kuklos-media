{
  "name": "kuklos-media",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages"
  },
  "dependencies": {
    "core-js": "^3.8.3",
    "nuxt": "^2.14.12",
    "push-dir": "^0.4.1"
  },
  "devDependencies": {
    "@nuxtjs/vuetify": "^1.11.3"
  }
}
